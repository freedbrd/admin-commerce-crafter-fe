import { AvailableResourcesItemsPipe } from './available-resources-items.pipe';

describe('AvailableResourcesItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailableResourcesItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
